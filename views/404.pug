extends layout

block content
  .container.text-center.mt-5
    img(src= image, alt="404 Error", style="max-width: 350px; filter: drop-shadow(0 0 12px #00bfff);")
    h1.text-danger.mt-4.display-4 Unauthorized Access 🚫
    h2.text-muted.mt-3 #{message}
    p.mt-2.text-secondary It seems like the page you're looking for doesn't exist.
    p.text-secondary Double-check the URL or return to safety.
    a.btn.btn-primary.mt-3(href='/') 🔙 Back to Home
